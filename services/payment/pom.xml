<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>vn.vnpt</groupId>
        <artifactId>side-project</artifactId>
        <version>1.0-SNAPSHOT</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>

    <artifactId>payment</artifactId>
    <packaging>jar</packaging>

    <name>payment</name>
    <description>PaymentService — owns outbound payment calls, FR-25 stable idempotency key (ADR-11, ADR-21)</description>

    <dependencies>
        <!--
          util: brings BOMs (Spring Boot 4.0.0, Spring Cloud 2025.1.0) transitively, plus
          BaseEntity / SnowflakeIdGenerator / @SoftUk / etc. Do NOT re-import the BOMs here —
          architecture-detail.md line 97.
        -->
        <dependency>
            <groupId>vn.vnpt</groupId>
            <artifactId>util</artifactId>
            <version>0.0.1-SNAPSHOT</version>
        </dependency>

        <!-- Web: embedded Tomcat for the runtime smoke (AC #11 /actuator/health on :8086). -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- JPA: Payment aggregate (payment_aggregate table). -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- Flyway: schema authority. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-flyway</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>

        <!-- Postgres JDBC driver (runtime). -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Actuator: /actuator/health. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- Spring Modulith: @ApplicationModule on PaymentApplication.java (ADR-01). -->
        <dependency>
            <groupId>org.springframework.modulith</groupId>
            <artifactId>spring-modulith-starter-core</artifactId>
        </dependency>

        <!--
          Modulith outbox bridge — required so the Spring Modulith autoconfig chain can wire the
          EventPublicationRegistry (StalenessMonitorConfiguration needs it). The bridge's
          JdbcEventPublicationAutoConfiguration is excluded (mirrors catalog / cart / checkout);
          util's ModulithBridgeSupport provides the no-op EventPublicationRepository + EventSerializer
          beans via PaymentModulithOutboxPublisher (no @Component on the abstract base; concrete
          subclass needed). Story 3.5 wires the real outbox writes + HMAC signing.
        -->
        <dependency>
            <groupId>org.springframework.modulith</groupId>
            <artifactId>spring-modulith-events-jdbc</artifactId>
        </dependency>

        <!-- Test scope -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- ArchUnit: PaymentPortContractTest (Task 6). -->
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Testcontainers pinned to 1.20.4 — matches catalog / checkout / inventory. -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <version>1.20.4</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>1.20.4</version>
            <scope>test</scope>
        </dependency>

        <!--
          ponytail: util/pom.xml declares Lombok as <optional>true</optional> so it does NOT
          propagate transitively. Declare Lombok directly. Version matches util's.
        -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>1.18.42</version>
            <scope>provided</scope>
        </dependency>
        <!--
          ponytail: NO com.stripe:stripe-java dep in Story 3.1 — the real SDK wiring (Vault key,
          @ConfigurationProperties("stripe.*"), RequestOptions.setIdempotencyKey, webhook signature
          verification) lands in Story 3.3 (Elements iframe). This story is test-double-only; the
          key contract is what we test.
        -->
        <!--
          Story 3.3: Stripe SDK wiring (FR-24, FR-29, ADR-23, R-15 mitigation).
          Pinned to a specific minor per addendum.md R-12 (Stripe API drift mitigation).
          stripe-java 28.0.0 ships with Java 25 / Spring Boot 4 support; verify at upgrade time.
        -->
        <dependency>
            <groupId>com.stripe</groupId>
            <artifactId>stripe-java</artifactId>
            <version>28.0.0</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.1</version>
                <configuration>
                    <release>25</release>
                    <compilerArgs>
                        <arg>-parameters</arg>
                    </compilerArgs>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>1.18.42</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>4.0.0</version>
                <!-- NO <skip>true</skip> — spring-boot:run must work for AC #11 runtime smoke. -->
            </plugin>
        </plugins>
    </build>
</project>