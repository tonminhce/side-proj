<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>vn.vnpt</groupId>
        <artifactId>side-project</artifactId>
        <version>1.0-SNAPSHOT</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>

    <artifactId>catalog</artifactId>
    <packaging>jar</packaging>

    <name>catalog</name>
    <description>CatalogService — owns products, variants, attributes (ADR-01, ADR-03, ADR-14)</description>

    <dependencies>
        <!--
          util: brings BOMs (Spring Boot 4.0.0, Spring Cloud 2025.1.0) transitively.
          All Spring Boot starter versions below inherit from util/pom.xml <dependencyManagement>.
          Do NOT re-import the BOMs here — architecture-detail.md line 97.
        -->
        <dependency>
            <groupId>vn.vnpt</groupId>
            <artifactId>util</artifactId>
            <version>0.0.1-SNAPSHOT</version>
        </dependency>

        <!-- Web: needed so @SpringBootApplication produces a runnable jar with embedded Tomcat
             (Story 1.4's admin read view needs the HTTP listener). -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- JPA: needed for Story 1.2's Product/Variant/Attribute entities. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- Flyway: schema authority. Spring Boot 4 split Flyway autoconfig into its own
             module (Spring Boot 4.0 Migration Guide); the flyway-core / flyway-database-postgresql
             libs alone don't trigger autoconfig. spring-boot-flyway brings FlywayAutoConfiguration
             which wires Flyway into the DataSource lifecycle. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-flyway</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>

        <!-- Postgres JDBC driver (runtime). -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Actuator: AC #8 /actuator/health endpoint. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- Spring Modulith: enables @ApplicationModule on CatalogApplication.java
             (architecture.md line 714 / line 884). Story 1.4 wires ApplicationModules.verify(). -->
        <dependency>
            <groupId>org.springframework.modulith</groupId>
            <artifactId>spring-modulith-starter-core</artifactId>
        </dependency>

        <!--
          Modulith outbox bridge — Story 1.3 (ADR-01 / ADR-14 / ADR-20).
          Brings in @ApplicationModuleListener (in-process) and JdbcOutboxChannel
          (Kafka publish via the bridge's poller). Version inherited from
          root pom.xml <spring-modulith.version> (2.0.7).
        -->
        <dependency>
            <groupId>org.springframework.modulith</groupId>
            <artifactId>spring-modulith-events-jdbc</artifactId>
        </dependency>

        <!-- Test scope -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- ArchUnit: package-boundary test (Task 7). Version inherited from root pom.xml dependencyManagement. -->
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <scope>test</scope>
        </dependency>

        <!--
          Testcontainers: prefer over H2 (AC #11). Pinned to 1.20.4.
          Story 0.4's CI scaffold does not register a Testcontainers base class; tests inline
          the PostgreSQLContainer. Fall back to H2 (PostgreSQL mode) only on first CI failure.
        -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <version>1.20.4</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>1.20.4</version>
            <scope>test</scope>
        </dependency>

        <!-- ponytail: util/pom.xml declares Lombok as <optional>true</optional>, so it does NOT
             propagate transitively to consumers. Story 1.2 is the first catalog entity that uses
             Lombok annotations — declare Lombok directly here. Version matches util's. -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>1.18.42</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.1</version>
                <configuration>
                    <release>25</release>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>1.18.42</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>4.0.0</version>
                <!-- Story 1.1: NO <skip>true</skip> — spring-boot:run must work. -->
            </plugin>
            <!--
              Avro codegen — Story 1.3 (FR-5, NFR-MIG-2, ADR-15).
              Generates Java POJOs from services/catalog/src/main/avro/*.avsc into
              target/generated-sources/avro/. The generated types live at the same FQN
              as the hand-written record they replace (vn.vnpt.catalog.domain.event.*).
              <stringType>String</stringType> override prevents the default Utf8 type —
              the use case compiles without .toString() at every site.
            -->
            <plugin>
                <groupId>org.apache.avro</groupId>
                <artifactId>avro-maven-plugin</artifactId>
                <version>1.12.0</version>
                <executions>
                    <execution>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>schema</goal>
                        </goals>
                        <configuration>
                            <sourceDirectory>${project.basedir}/src/main/avro</sourceDirectory>
                            <outputDirectory>${project.build.directory}/generated-sources/avro</outputDirectory>
                            <stringType>String</stringType>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>